#include <bits/stdc++.h>

using namespace std;
vector<string> rowsOfStacks;
vector<string> stacks;
int main()
{
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
    string line;
    cin>>line;
    //read the stacks
    while (!line.empty()){
        rowsOfStacks.push_back(line);
    }
    stack.resize(rowsOfStacks[0].length()/3);

    //read the moves
    return 0;
}
