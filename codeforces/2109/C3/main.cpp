#include <bits/stdc++.h>

using namespace std;

int t, n;

int main() {
    cin>>t;
    int tmp;
    while (t--) {
        cin>>n;
        cout<<"mul 999999999"<<endl;
        cout.flush();
        cin>>tmp;
        cout<<"digit"<<endl;
        cout.flush();
        cin>>tmp;
        if (n != 81) {
            cout<<"add "<<n-81<<endl;
            cout.flush();
            cin>>tmp;
        }
        cout<<"!"<<endl;
        cout.flush();
        cin>>tmp;
    }
    return 0;
}
