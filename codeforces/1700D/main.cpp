#include <bits/stdc++.h>

using namespace std;
int n, q, res;
vector<int> v, t;
int main()
{
    cin>>n;
    v.resize(n);
    for (int i=0; i<n; i++){
        cin>>v[i];
    }
    cin>>q;
    t.resize(q);
    for (int i=0; i<q; i++){
        cin>>t[i];
    }

    res=0;
    cout<<res;
    return 0;
}
