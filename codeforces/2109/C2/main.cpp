#include <bits/stdc++.h>

using namespace std;

int t, n;

int main() {
    cin>>t;
    int tmp;
    while (t--) {
        cin>>n;
        cout<<"mul 9"<<endl;
        cout.flush();
        cin>>tmp;
        cout<<"digit"<<endl;
        cout.flush();
        cin>>tmp;
        cout<<"digit"<<endl;
        cout.flush();
        cin>>tmp;
        cout<<"add "<<n-9<<endl;
        cout.flush();
        cin>>tmp;
        cout<<"!"<<endl;
        cin>>tmp;
    }
    return 0;
}
